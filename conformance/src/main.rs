//! gx-conformance
//!
//! Status: **Planned**. Skeleton only: no implementation, and nothing here
//! claims otherwise (docs/README.md status vocabulary).
//!
//! One suite per plane, runnable against a mock or a real peer. Reports SKIP rather than PASS when a capability is absent (invariant 43).

#![forbid(unsafe_code)]

fn main() {
    eprintln!("gx-conformance: Planned. Not implemented.");
    std::process::exit(78); // EX_CONFIG: configured, not runnable yet.
}
