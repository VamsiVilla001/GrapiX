//! gx-mock-engine
//!
//! Status: **Planned**. Skeleton only: no implementation, and nothing here
//! claims otherwise (docs/README.md status vocabulary).
//!
//! A valid engine peer for Editor and Playout development. Must refuse exactly where the real engine refuses; a permissive mock teaches a contract that does not exist.

#![forbid(unsafe_code)]

fn main() {
    eprintln!("gx-mock-engine: Planned. Not implemented.");
    std::process::exit(78); // EX_CONFIG: configured, not runnable yet.
}
